{
  "name": "cairn-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cairn control plane — Cloudflare Workers gateway + Durable Objects fleet state (spec §7).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
