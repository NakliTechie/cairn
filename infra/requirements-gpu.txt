# Cairn GPU box image — pinned, no surprise downloads (handoff §6: probe + pin every dep).
# The exact CUDA build of torch is box/driver-specific (shard's pyproject note) — pin it to
# the g6/L4 driver on first bring-up, then freeze here. Versions below are PLACEHOLDERS to
# confirm on the box (rung 2).

# per-node serving engine — we wrap it at block granularity, don't rebuild it
sglang==0.4.*            # TODO pin exact (the block-forward path is SGLang-version-specific)
torch==2.*               # TODO pin the exact CUDA wheel for the L4 driver

# the wire (fork/shard/wire.py) — required to load any node
cryptography>=42
numpy>=1.26

# model/weight pull
huggingface-hub>=0.24
transformers>=4.44       # rung-2 reference-block stepping stone (sglang_node.py path C)

# the Cairn scheduler/runtime (installed from the repo, editable on the box)
pyyaml>=6.0
